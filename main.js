let name = prompt('ismingizni kiriting')
let id = prompt('space idenini kiriting')
let password = prompt('space passwordni kiriting')   

const obj ={
    name : name,
    id: id,
    password: password
   
}


console.log(obj);